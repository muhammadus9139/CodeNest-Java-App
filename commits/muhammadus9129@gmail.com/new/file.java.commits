Update file.java|file.java|2026-05-03 01:04:19|// Start coding here...{{NL}}public SearchScreen(Stage stage, User currentUser, UserStorage userStorage,{{NL}}                    RepoStorage repoStorage, CommitStorage commitStorage, IssueStorage issueStorage) {{{NL}}    this.stage = stage;{{NL}}    this.currentUser = currentUser;{{NL}}    this.userStorage = userStorage;{{NL}}    this.repoStorage = repoStorage;{{NL}}    this.commitStorage = commitStorage;{{NL}}    this.issueStorage = issueStorage;{{NL}}    this.searchEngine = new SearchEngine();{{NL}}{{NL}}    ArrayList<Repo> allRepos = repoStorage.getPublicRepos();{{NL}}    ArrayList<Repo> userRepos = repoStorage.getUserRepos(currentUser.getUsername());{{NL}}    for (Repo r : userRepos) {{{NL}}        if (!allRepos.contains(r)) allRepos.add(r);{{NL}}    }{{NL}}    searchEngine.loadRepos(allRepos);{{NL}}}
